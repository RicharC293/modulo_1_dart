void main() {
  // Igual(Equals) = (le asigno)
  final a = 10;

  // Operador null aware ??= (asignalo en caso de que sea null)
  String? nombre = 'Richar';

  nombre ??= 'Unknow';

  print(nombre);

  // Operador de asignacion += (suma el valor indicado a la variable)
  double c = 20;
  c += -5;
  print(c);

  // operador asignacion -= (resta el valor indicado a la variable)
  c -= 5;
  print(c);

  // *= (multiplica el valor indicado a la variable)
  c *= 2;
  print(c);

  // /= (divide el valor indicado a la variable)
  c /= 10;
  print(c);

  /// Implementar la division entera y el modulo 
  /// 
  /// ----------------------------------------
  /// Pendiente tarea
  /// ----------------------------------------
  /// 

}
